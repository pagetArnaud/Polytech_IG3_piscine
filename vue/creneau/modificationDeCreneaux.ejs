<%- include('../commun/head'); %>

<body>

    <label for="site-search">Nom du groupe</label>
    <input type="search" id="id" name="id"
           aria-label="Search through site content">

<table class="table">
    <thead>
    <tr>
        <th scope="col">date</th>
        <th scope="col">heure</th>
        <th scope="col">salle</th>
        <th scope="col"></th>
    </tr>
    </thead>
    <tbody id="listeCreneau">
    <% data.forEach(function(row) { %>
        <% var date = new Date(row.date); %>
        <tr>
            <td><%= date.getDate() %>/<%= date.getMonth() + 1 %>/<%= date.getFullYear() %></td>
            <td><%= row.heureDebut %></td>
            <td><%= row.salle %></td>
            <td>
                <form action="" method="post">
                    <input type="hidden" id="num" name="num" value="<%= row.num %>">
                    <button class="button" type="submit">Choisir</button>
                </form>
            </td>
        </tr>
    <% }); %>
    </tbody>
</table>

</body>

<%- include('../commun/footer'); %>
